import React from "react";

// TEMPORAL: Componente deshabilitado por incompatibilidad con react-joyride/React 18+
// TODO: Reintegrar tour interactivo cuando haya compatibilidad estable.

export default function DashboardTour() {
  return (
    <div style={{ padding: 16, textAlign: "center", color: "#888" }}>
      (Tour interactivo deshabilitado temporalmente por mantenimiento)
    </div>
  );
}
